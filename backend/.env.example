# Database (async driver for SQLAlchemy)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/sisuiq
DATABASE_ECHO=false

# Vector Database
QDRANT_URL=http://localhost:6333
QDRANT_COLLECTION=sisuiq_chunks

# OpenAI
OPENAI_API_KEY=sk-your-api-key
OPENAI_EMBED_MODEL=text-embedding-3-small
OPENAI_CHAT_MODEL=gpt-4o

# Storage paths
STORAGE_PATH=storage

# CORS
CORS_ORIGINS=http://localhost:3000

# Environment
ENVIRONMENT=development

# Demo user (for development)
DEMO_USER_EMAIL=demo@uetcl.go.ug

# Admin token (DEMO-ONLY - change in production)
ADMIN_TOKEN=demo-admin-token-change-me
